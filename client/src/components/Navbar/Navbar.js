import React from 'react';

const Navbar = (props) => {
    return (
        <nav class="navbar navbar-light bg-warning">
            <a class="navbar-brand mx-auto" href="#">Genomic Study Database</a>
        </nav>
    )
};

export default Navbar;